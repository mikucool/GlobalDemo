package com.example.globaldemo.ad.constant

enum class RewardAdState {
    DEFAULT,
    LOADING,
    LOAD_ERROR,
    LOADED,
    DISPLAYED,
    DISPLAY_ERROR,
    REWARDED,
    CLOSED
}

enum class InterstitialAdState {
    DEFAULT,
    LOADING,
    LOAD_ERROR,
    LOADED,
    DISPLAYED,
    DISPLAY_ERROR,
    CLOSED
}