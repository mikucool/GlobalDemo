package com.example.globaldemo.ui.screen.test

enum class TestRewardAdState {
    DEFAULT,
    LOADING,
    LOADED,
    LOAD_ERROR,
    DISPLAYED,
    DISPLAY_ERROR,
    CLOSED,
    REWARDED
}

enum class TestInterstitialAdState {
    DEFAULT,
    LOADING,
    LOADED,
    LOAD_ERROR,
    DISPLAYED,
    DISPLAY_ERROR,
    CLOSED,
}
